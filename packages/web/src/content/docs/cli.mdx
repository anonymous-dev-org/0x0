---
title: CLI
description: Terminal Agent CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Terminal Agent CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
0x0
```

But it also accepts commands as documented on this page. This allows you to interact with Terminal Agent programmatically.

```bash
0x0 run "Explain how closures work in JavaScript"
```

---

### tui

Start the Terminal Agent terminal user interface.

```bash
0x0 [project]
```

#### Flags

| Flag         | Short | Description                                                             |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue the last session                                               |
| `--session`  | `-s`  | Session ID to continue                                                  |
| `--fork`     |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--prompt`   |       | Prompt to use                                                           |
| `--model`    | `-m`  | Model to use in the form of provider/model                              |
| `--agent`    |       | Agent to use                                                            |
| `--port`     |       | Port to listen on                                                       |
| `--hostname` |       | Hostname to listen on                                                   |

---

## Commands

The Terminal Agent CLI also has the following commands.

---

### agent

Manage agents for Terminal Agent.

```bash
0x0 agent [command]
```

---

### attach

Attach a terminal to an already running Terminal Agent backend server started via `serve` or `web` commands.

```bash
0x0 attach [url]
```

This allows using the TUI with a remote Terminal Agent backend. For example:

```bash
# Start the backend server for web/mobile access
0x0 web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
0x0 attach http://10.20.30.40:4096
```

#### Flags

| Flag        | Short | Description                       |
| ----------- | ----- | --------------------------------- |
| `--dir`     |       | Working directory to start TUI in |
| `--session` | `-s`  | Session ID to continue            |

---

#### create

Create a new agent with custom configuration.

```bash
0x0 agent create
```

This command will guide you through creating a new agent with a custom system prompt and tool configuration.

---

#### list

List all available agents.

```bash
0x0 agent list
```

---

### auth

Command to manage credentials and login for providers.

```bash
0x0 auth [command]
```

---

#### login

Terminal Agent is powered by the provider list at [Models.dev](https://models.dev), so you can use `0x0 auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/0x0/auth.json`.

```bash
0x0 auth login
```

When Terminal Agent starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
0x0 auth list
```

Or the short version.

```bash
0x0 auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
0x0 auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
0x0 github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
0x0 github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
0x0 github run
```

##### Flags

| Flag      | Description                            |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event to run the agent for |
| `--token` | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
0x0 mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
0x0 mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
0x0 mcp list
```

Or use the short version.

```bash
0x0 mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
0x0 mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
0x0 mcp auth list
```

Or use the short version.

```bash
0x0 mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
0x0 mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
0x0 mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
0x0 models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
0x0 models anthropic
```

#### Flags

| Flag        | Description                                                  |
| ----------- | ------------------------------------------------------------ |
| `--refresh` | Refresh the models cache from models.dev                     |
| `--verbose` | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Terminal Agent.

```bash
0x0 models --refresh
```

---

### run

Run 0x0 in non-interactive mode by passing a prompt directly.

```bash
0x0 run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "0x0 run"
0x0 run Explain the use of context in Go
```

You can also attach to a running `0x0 serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
0x0 serve

# In another terminal, run commands that attach to it
0x0 run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag         | Short | Description                                                             |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--command`  |       | The command to run, use message for args                                |
| `--continue` | `-c`  | Continue the last session                                               |
| `--session`  | `-s`  | Session ID to continue                                                  |
| `--fork`     |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--share`    |       | Share the session                                                       |
| `--model`    | `-m`  | Model to use in the form of provider/model                              |
| `--agent`    |       | Agent to use                                                            |
| `--file`     | `-f`  | File(s) to attach to message                                            |
| `--format`   |       | Format: default (formatted) or json (raw JSON events)                   |
| `--title`    |       | Title for the session (uses truncated prompt if no value provided)      |
| `--attach`   |       | Attach to a running 0x0 server (e.g., http://localhost:4096)       |
| `--port`     |       | Port for the local server (defaults to random port)                     |

---

### serve

Start a headless Terminal Agent server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
0x0 serve
```

This starts an HTTP server that provides API access to 0x0 functionality without the TUI interface. Set `ZEROXZERO_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `0x0`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### session

Manage Terminal Agent sessions.

```bash
0x0 session [command]
```

---

#### list

List all Terminal Agent sessions.

```bash
0x0 session list
```

##### Flags

| Flag          | Short | Description                          |
| ------------- | ----- | ------------------------------------ |
| `--max-count` | `-n`  | Limit to N most recent sessions      |
| `--format`    |       | Output format: table or json (table) |

---

### stats

Show token usage and cost statistics for your Terminal Agent sessions.

```bash
0x0 stats
```

#### Flags

| Flag        | Description                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Show stats for the last N days (all time)                                   |
| `--tools`   | Number of tools to show (all)                                               |
| `--models`  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| `--project` | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
0x0 export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

---

### import

Import session data from a JSON file or Terminal Agent share URL.

```bash
0x0 import <file>
```

You can import from a local file or an Terminal Agent share URL.

```bash
0x0 import session.json
0x0 import https://opncd.ai/s/abc123
```

---

### web

Start a headless Terminal Agent server with a web interface.

```bash
0x0 web
```

This starts an HTTP server and opens a web browser to access Terminal Agent through a web interface. Set `ZEROXZERO_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `0x0`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
0x0 acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag         | Description           |
| ------------ | --------------------- |
| `--cwd`      | Working directory     |
| `--port`     | Port to listen on     |
| `--hostname` | Hostname to listen on |

---

### uninstall

Uninstall Terminal Agent and remove all related files.

```bash
0x0 uninstall
```

#### Flags

| Flag            | Short | Description                                 |
| --------------- | ----- | ------------------------------------------- |
| `--keep-config` | `-c`  | Keep configuration files                    |
| `--keep-data`   | `-d`  | Keep session data and snapshots             |
| `--dry-run`     |       | Show what would be removed without removing |
| `--force`       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates 0x0 to the latest version or a specific version.

```bash
0x0 upgrade [target]
```

To upgrade to the latest version.

```bash
0x0 upgrade
```

To upgrade to a specific version.

```bash
0x0 upgrade v0.1.48
```

#### Flags

| Flag       | Short | Description                                                       |
| ---------- | ----- | ----------------------------------------------------------------- |
| `--method` | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The 0x0 CLI takes the following global flags.

| Flag           | Short | Description                          |
| -------------- | ----- | ------------------------------------ |
| `--help`       | `-h`  | Display help                         |
| `--version`    | `-v`  | Print version number                 |
| `--print-logs` |       | Print logs to stderr                 |
| `--log-level`  |       | Log level (DEBUG, INFO, WARN, ERROR) |

---

## Environment variables

Terminal Agent can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `ZEROXZERO_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `ZEROXZERO_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `ZEROXZERO_CONFIG`                     | string  | Path to config file                               |
| `ZEROXZERO_CONFIG_DIR`                 | string  | Path to config directory                          |
| `ZEROXZERO_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `ZEROXZERO_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `ZEROXZERO_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `ZEROXZERO_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `ZEROXZERO_PERMISSION`                 | string  | Inlined json permissions config                   |
| `ZEROXZERO_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `ZEROXZERO_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `ZEROXZERO_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `ZEROXZERO_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `ZEROXZERO_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `ZEROXZERO_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `ZEROXZERO_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `ZEROXZERO_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `ZEROXZERO_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `ZEROXZERO_DISABLE_FILETIME_CHECK`     | boolean | Disable file time checking for optimization       |
| `ZEROXZERO_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `ZEROXZERO_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `ZEROXZERO_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `ZEROXZERO_SERVER_USERNAME`            | string  | Override basic auth username (default `0x0`) |
| `ZEROXZERO_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `ZEROXZERO_EXPERIMENTAL`                         | boolean | Enable all experimental features        |
| `ZEROXZERO_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `ZEROXZERO_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `ZEROXZERO_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `ZEROXZERO_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `ZEROXZERO_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `ZEROXZERO_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `ZEROXZERO_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `ZEROXZERO_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `ZEROXZERO_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `ZEROXZERO_EXPERIMENTAL_LSP_TY`                  | boolean | Enable experimental LSP type checking   |
| `ZEROXZERO_EXPERIMENTAL_MARKDOWN`                | boolean | Enable experimental markdown features   |
| `ZEROXZERO_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
